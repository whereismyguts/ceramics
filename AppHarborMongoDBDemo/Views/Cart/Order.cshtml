@model CartItems
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
</head>
<div class="contact">
    
    <h1>Оформление заказа</h1>
    
    

    <h2>Подробности</h2>
    <form method="post">
        <div class="cart-sidebar order-cart"></div>
        @*@Html.Partial("~/Views/Shared/_CartItemsList.cshtml", Model)*@
        <input name="client_name" required="required" type="text" placeholder="Представтесь" />
        <textarea name="comment" required="required" placeholder="Комментарий к заказу, контактные данные"></textarea>






        <style>
            .order-cart {
                position: initial;
                width: auto;
                max-width: 100%;
                margin: auto;
            }

            #show-cart, .cart-sidebar:not(.order-cart), #process-order, #hide-cart {
                display: none;
            }
        </style>

        @*<script src="~/Scripts/bundle.js"></script>
            <script>
                $('.refresh-cart').click(refreshCartSimple)
                $(document).ready(refreshCartSimple);

                function refreshCartSimple() {
                    alert('refr');
                    $.ajax({
                        type: "POST",
                        url: "/Cart/GetCartItemsView",
                        success: function (res) {
                            if (res) {
                                $('.order-cart').empty();
                                $('.order-cart').append(res);
                            }
                        },
                        datatype: "json",
                        traditional: true
                    });
                }

            </script>*@
        @*@foreach(var item in Model.Items) {
                <p>@item.Thing.Name</p>
            }
                <button style="width:70px; float:right" type="submit">OK</button>*@
        @if(Model.Count == 0) {
            <a href="/" class="button">Что у вас есть?</a>
        }
        else {
        <button class="button"  type="submit">Я беру!</button>
        }

    </form>
    
</div>